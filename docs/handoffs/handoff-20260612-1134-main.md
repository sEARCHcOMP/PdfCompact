# [Handoff] リファクタ Phase 0→1→2(高価値部完了) — 2026-06-12 11:34 (branch: main)

## Goal / Scope
PDF Compact(単一HTML・ブラウザ完結PDFツール)の完全リファクタリングを Phase 0〜5 で進める。
鉄則は「作り方だけ変える。動き方は1ピクセルも変えない」。配布物は単一HTMLのまま。
今セッションは Phase 0(安全網)・Phase 1(ビルド工程)・Phase 2(共通基盤抽出の高価値部)を完遂。

## Done (今セッション完了)
- [x] **Phase 0 安全網**: Playwright スモークテスト基盤 + リリース自動化(build/release.ps1)+ 死蔵4件削除
- [x] **Phase 1 ビルド工程**: src/ に分割(styles.css / guide.html / index.template.html / app/*.js)+ 連結only の build.js(byte一致をSHA検証)。app.js を 7モジュールに分割(00-core/10-img2pdf/20-convert/30-pdfedit/40-imgplace[4,834行]/50-redact/90-settings)
- [x] **Phase 2 共通基盤(5増分)**:
  - (1) triggerDownload 4実装→1
  - (2) escapeHtml 4実装→1(String()強化込み)
  - (3) **viewportToPage 統合 + ゴールデン10ケース**(回転4×CropBox2 + 黒塗り2)= 最重要・最高リスク完了
  - (4) formatSize→formatBytes
  - (5) **EXIF orientation パーサ統合 + 等価性ゴールデン**(3実装×orientation1-8×両エンディアン)= 要慎重項目完了
- [x] テストは 15 → **27本**。`npm test` で全green(2 flaky はリトライ救済)

## In Progress
なし(各増分はキリよくコミット済み・作業ツリークリーン)。

## Pending (= 次にやる候補。どれも「ゴールデン先張り→慎重統合」の集中タスク)
- [ ] **setStatus / failedNames 統合**: モード別DOM(#imgStatusMsg / #editStatusMsg 等)なのでパラメータ化が要る中規模リファクタ。handoff証拠B(エラー即消しの3モードコピペ)に対応
- [ ] **catch(_e){} 35箇所の棚卸し**: ①notifyWarnすべき ②failedNamesに積むべき ③理由コメント付きで握ってOK、の3分類。低リスクだが地味
- [ ] **00-core の軽量化モード絡みほぐし**: 軽量化だけ独立IIFEになっておらず共有基盤と混在(Phase 3寄り)
- [ ] Phase 3(モード統一構造)/ Phase 4(取説・CSS整理)/ Phase 5(テスト拡充・ARCHITECTURE.md)

## やらないと確定(精査済み・蒸し返さない)
- **BusyLock 統合**: 実行中フラグ4つは2概念混在(長処理ロック=isConverting/st.exporting、描画レース制御=st.rendering[renderGen相棒]/isRendering[renderPending相棒])。相棒機構が別で統合は壊す
- **暗号化検出 統合**: モードごとに意図的に違う(サニタイズ=絶対平文化しない/pdfedit=getPermissions→PROTECTED_PDF/imgplace等=ignoreEncryption:true)。deliberate divergence

## Key files
- `docs/plans/refactoring-handoff.md` — 深い暗黙知。**Phase 2 続き着手前に必読**(物理法則・検証作法・Phase 2 で分かった判断を全部記録)
- `docs/plans/refactoring-roadmap.md` — Phase 0〜5 全体計画
- `build.js` — 連結ビルド。`node build.js` で生成 / `node build.js --check` で byte一致SHA検証
- `src/app/*.js` — モジュール分割済みアプリJS。**今後コードは src/ を編集**
- `src/app/00-core.js` — 共有helper群(triggerDownload/escapeHtml/formatBytes/viewportRectToPageDrawOpts/readExifOrientation)が script トップレベルにあり全モードから可視
- `tests/` — スモーク27本。`tests/geometry.spec.js`(座標写像ゴールデン)/`tests/exif.spec.js`(EXIF等価性)
- `build/release.ps1` — `pwsh build/release.ps1 -Version x.y.z -Notes "..."` でリリース一気通貫

## Gotchas / 注意点
- **今後の編集ルール**: src/ を編集 → `node build.js` → bundle も一緒に commit。`npm test` は pretest で build:check が走る(src↔bundle 乖離をテスト前に検出)
- **Phase 2 の主力パターンは完了**: 「script トップレベル共有版に寄せてモードの shadow 削除」は全部やり切った(grep でモード再定義ゼロ確認済み)。次は質が変わる(setStatus等はパラメータ化要)
- **byte一致はもう基準でない**(Phase 2 から実コード改修)。代わりに **27テストが防波堤**。バグ修正とリファクタは別コミットに
- 家Mac初回: `npm i && npx playwright install chromium` が要る
- CRLF / プレビュー劣化時はサーバ再起動 / 黒塗り出力は生バイト走査で秘密ゼロ確認(全部テストに組込済み)
- 単一HTML・依存ゼロ追加・bundler/minify不採用は製品の物理法則(好みでない)

## 次の最初の一手
家(or 次回)で `git pull` → `docs/plans/refactoring-handoff.md` の Phase 2 節を読む → `npm test` が27本green を確認 → setStatus/failedNames 統合に着手(まず各モードの status 要素IDと呼び出し箇所を実測 → 共有 setStatus(el, text, type) を設計 → 1モードずつ移植してテスト)。または catch棚卸し(低リスク)から軽く再開でも可。

## 関連リンク
- 前回 handoff: docs/handoffs/handoff-20260611-2240-main.md(Phase 0 完了時)
- リポ: https://github.com/sEARCHcOMP/PdfCompact (main = v3.8.0 配布中、bundle は機能不変)
