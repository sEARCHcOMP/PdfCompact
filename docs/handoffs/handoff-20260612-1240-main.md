# [Handoff] リファクタリング完了(Phase 0-3 走破・以降は意図的に打ち切り) — 2026-06-12 12:40 (branch: main)

## Goal / Scope
PDF Compact の構造的負債の解消。「作り方だけ変える。動き方は1ピクセルも変えない」を貫き、
**価値のある所で止める**(ユーザー判断: 無理なリファクタはやらない)。

## Done (リファクタ全体の最終形)
- [x] **Phase 0 安全網**: Playwright テスト基盤 / release.ps1 / 死蔵4件削除
- [x] **Phase 1 ビルド工程**: src/ 分割 + 連結build.js(byte一致SHA検証)+ app.js 7分割
- [x] **Phase 2 共通基盤(6統合)**: download / escapeHtml / **座標写像+ゴールデン10** / formatSize / **EXIF+等価性ゴールデン** / setStatusミラー。「2実装で同バグ2回」を両方解消
- [x] **Phase 3 モジュール化**: imgPlace(4,772行)→7分割、00-core(1,889行)→5分割。**全17モジュール ≤966行**
- [x] **docs/ARCHITECTURE.md 新設**(ファイル地図・データフロー・共通helper所在・検証作法)
- [x] テスト 0→**27本**。配布版 bundle は機能不変(v3.8.0 のまま、同僚への更新通知なし)

## 意図的にやらないと確定した項目(蒸し返さない)
- デッドコード削除(動的参照で誤検知リスク大・reward小)
- lifecycle 正規化(高churn・低価値)
- BusyLock 統合 / 暗号化検出統合(deliberate divergence、統合すると壊す)
- failedNames 統合(モード別の意味あるメッセージ差)/ catch(_e){} 35箇所棚卸し(全部正当な防御)
- **Phase 4(CSS死にスタイル掃除)/ Phase 5(テスト拡充)は未着手のまま任意**。やるなら新セッションで「検出のみ→承認→削除」の慎重手順で

## In Progress
なし。作業ツリークリーン、全コミット push 済み。

## Pending (リファクタ外の通常開発に戻る)
- [ ] 同僚からの v3.8.0(黒塗り)フィードバック対応
- [ ] rank2 機能: 電子印鑑(ハンコ画像配置)— imgPlace 配置エンジン流用。共有化した viewportRectToPageDrawOpts がそのまま効く
- [ ] 取説に黒塗りの PART 追加(v3.8.0 で未対応のまま)

## Key files
- `docs/ARCHITECTURE.md` — **まずこれ**。構造の地図(新セッションの入口)
- `docs/plans/refactoring-handoff.md` — 深い暗黙知(物理法則・検証作法・全Phase の実地判断)
- `build.js` / `build/release.ps1` / `tests/` — 開発の三種の神器
- `src/app/*.js` — 17モジュール。**今後の編集は src/ → node build.js → bundle も一緒に commit**

## Gotchas
- `npm test` は pretest で build:check(src↔bundle 乖離検出)。家Mac初回は `npm i && npx playwright install chromium`
- 機能開発に戻ったら: 修正は該当モジュールだけ触る→build→`npm test`→release.ps1。座標/EXIF/黒塗りを触ったら geometry/exif/redact spec が番犬
- 単一HTML・依存ゼロ・bundler不採用は物理法則

## 次の最初の一手
リファクタは完了。次セッションは通常開発(黒塗りフィードバック or 電子印鑑 or 取説PART)から。
入口は `docs/ARCHITECTURE.md` → 必要なら refactoring-handoff.md。

## 関連リンク
- 前回 handoff: docs/handoffs/handoff-20260612-1134-main.md
- リポ: https://github.com/sEARCHcOMP/PdfCompact (main = v3.8.0 配布中)
